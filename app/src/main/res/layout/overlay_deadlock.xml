<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#CC1A0000"
    android:padding="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/deadlock_title"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFFF5252"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/deadlock_message"
        android:textSize="15sp"
        android:textColor="#CCFFFFFF"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_deadlock_undo"
        android:layout_width="200dp"
        android:layout_height="52dp"
        android:text="@string/undo"
        android:textColor="#FF0D1B2A"
        app:backgroundTint="#FFFFD700"
        app:cornerRadius="26dp"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_deadlock_restart"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="200dp"
        android:layout_height="48dp"
        android:text="@string/restart"
        android:textColor="#FFFFFFFF"
        app:strokeColor="#AAFFFFFF"
        app:cornerRadius="24dp" />

</LinearLayout>
